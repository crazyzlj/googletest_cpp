#ifndef DEMO_H_
#define DEMO_H_

// Returns n! (the factorial of n).  For negative n, n! is defined to be 1.
int Factorial(int n);

// Returns true if n is a prime number.
bool IsPrime(int n);

// Returns n * 2
int Double(int n);

#endif  // DEMO_H_
